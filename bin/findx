#!/usr/bin/env bash

# --color=auto for grep is set by dotfiles export GREP_OPTIONS='--color=auto'
# find . -name .svn -a -type d -prune -o -type f -print0 |  xargs -0 grep "$*"
# find   . -name .git -a -type d -prune -o -type f -print0 |  xargs -0 grep "$*"
find   . -name .git -a -type d -prune -o -type f -print0 |  xargs -0 grep --binary-files=without-match "$*"
